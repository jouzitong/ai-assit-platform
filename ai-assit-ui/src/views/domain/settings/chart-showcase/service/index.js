import { chartExamples, pageDescription, pageTitle, usageTips } from '../data'

export function useChartShowcasePage() {
  return {
    pageTitle,
    pageDescription,
    chartExamples,
    usageTips
  }
}
