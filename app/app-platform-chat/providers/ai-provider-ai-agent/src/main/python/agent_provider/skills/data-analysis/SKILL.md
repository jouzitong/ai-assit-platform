# Data Analysis

Use this package as the local skeleton for data analysis skills.

Keep deterministic parsing, validation, and analysis helpers in `scripts/`. Put
business rules and examples in `references/`, and machine-readable schemas in `assets/`.
