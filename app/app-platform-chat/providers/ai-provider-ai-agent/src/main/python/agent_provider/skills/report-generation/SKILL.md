# Report Generation

Use this package as the local skeleton for report generation skills.

Keep reusable report templates under `assets/`, factual writing guidance under
`references/`, and deterministic rendering helpers under `scripts/`.
