[project]
name = "ai-agent-provider"
version = "0.1.0"
description = "Python runtime for the ai-provider-ai-agent module"
requires-python = ">=3.11"
dependencies = [
  "openai-agents==0.19.2",
  "quickjs==1.19.4",
]
