package ai.platform.aiassit.service.ai.api.memory.enums;

/** Neutral memory categories supported by the provider contract. */
public enum MemoryType {
    RAW,
    SEMANTIC,
    EPISODIC,
    PROCEDURAL
}
