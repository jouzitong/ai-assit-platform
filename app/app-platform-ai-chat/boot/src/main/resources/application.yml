spring:
  application:
    name: aiChat
  profiles:
    active: dev
    include: common, lib
  main:
    allow-bean-definition-overriding: true
server:
  port: 13102
  servlet:
    context-path: /aiChat
mybatis-plus:
  configuration:
    default-enum-type-handler: org.athena.framework.data.mybatis.handler.DefaultEnumTypeHandler

database:
  name: 'ai_assist_ai_chat'