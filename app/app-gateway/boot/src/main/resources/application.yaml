server:
  port: 9764
spring:
  profiles:
    active: dev
    include: common, lib
  application:
    name: gateway
  main:
    allow-bean-definition-overriding: true
app:
  platform-user:
    name: 'app-platform-user'
  gateway:
    name: 'gateway'